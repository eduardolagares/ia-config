---
description: Use cases — Dry::Monads Result, domain NS, no dry-transaction
globs: app/domains/**/use_cases/**/*.rb
---
# Use cases

- **When:** one class per controller action / `ApplicationJob` that mutates state or orchestrates steps. **Else:** read-only relation → query object (`baladapp-query_objects.mdc`); single domain yes/no → rule object (`baladapp-rule_objects.mdc`).
- **Name:** `VerbNoun`. **NS:** `DominioX::UseCases::Y::`. **Path:** `app/domains/<domain>/use_cases/**/verb_noun.rb` ↔ nested `Y::` (same as `rules/`, `queries/`).

## Invariants

- `include Dry::Monads[:result, :do]` on the class. No app base use-case class. No `dry-transaction`.
- `def self.call(...) = new(...).call`.
- Input: kwargs or `Dry::Struct` — never `call({ only: "hash" })`.
- Every `yield`-ed callee returns `Success` | `Failure`. No `yield x if condition`.
- Branching work → private method returning Result. Side-effect skips → `maybe_*` methods; **only** `Success` (value or `nil`).
- `Failure(:meaningful_symbol)`. No `render`, HTTP status, or I18n in the use case.
- Heavy reuse → delegate to another use case `.call`. Policy / domain gate → `::DominioX::Rules::Y::Z.result(...)` (or `#result`), no duplicated rule bodies.
- Prefer records/objects from caller over raw ids when the caller can resolve them.

## Callers

- Controllers / jobs: load, Pundit, then `::DominioX::UseCases::...::Foo.call(...)`, map `Success`/`Failure` to response (see `baladapp-controllers.mdc`). Use case never owns presentation.

```ruby
case ::DominioOrders::UseCases::Orders::CreateOrder.call(user:, event:)
in Success(order)   then # e.g. render json: order, status: :created
in Failure(:known)  then # map symbol → status/body
in Failure(other)   then # fallback
end
# alt: r = X.call(...); r.success? ? r.value! : r.failure
```

## `Result` (for branching / chaining)

- `Success(x)` / `Failure(x)` — constructors. `yield` — unwrap nested `Result` from callee; `Failure` stops `#call` (do-notation).
- `success?` / `failure?` — discriminate. `value!` — payload on `Success` only (raises on `Failure`). `value_or(default)` — payload or default on `Failure`.
- `fmap` — map inner `Success` value. `bind` — block returns another `Result` (flatMap).

## Do not

- Hash-as-input; conditional `yield`; `maybe_*` returning `Failure`; helpers returning non-`Result`; `< ApplicationUseCase`; file not under `…/use_cases/` mirroring NS; use case for trivial query; passing ids when objects are available at call site; presentation in use case.

## Reference skeleton

```ruby
module DominioOrders
  module UseCases
    module Orders
      class CreateOrder
        include Dry::Monads[:result, :do]
        def self.call(user:, event:) = new(user:, event:).call

        def initialize(user:, event:)
          @user = user
          @event = event
        end

        def call
          return Failure(:forbidden) unless ::DominioOrders::Rules::Orders::CustomerMayOrder.result(user: @user, event: @event)
          # yield steps returning Result; then Success(order) or Failure(:sym)
        end
      end
    end
  end
end
```
