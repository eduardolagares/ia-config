# ia-config — contexto de agente (global)

Ficheiro irmão de `GEMINI.md` no mesmo repositório **ia-config**. Convenções detalhadas: `../rules/*.mdc` (relativamente à pasta `antigravity/`). Comandos: `../commands/`.

Usa estes ficheiros como referência quando o trabalho for compatível (Rails, testes, convenções descritas nos `.mdc`).
